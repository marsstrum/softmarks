<%= form_for(@user_topic) do |f| %>
  <% if @user_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_topic.errors.count, "error") %> prohibited this user_topic from being saved:</h2>

      <ul>
      <% @user_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.number_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
